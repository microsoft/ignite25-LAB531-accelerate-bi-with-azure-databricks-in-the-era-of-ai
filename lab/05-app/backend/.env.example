# ===== DATABRICKS SQL WAREHOUSE CONNECTION =====
# Required: Your Databricks workspace server hostname (without https://)
# Example: adb-1234567890123456.7.azuredatabricks.net
DATABRICKS_SERVER_HOSTNAME=your-workspace.cloud.databricks.net

# Required: Databricks host (same as server hostname, used by OAuth2 TokenMinter)
DATABRICKS_HOST=your-workspace.cloud.databricks.net

# Option 1: Provide just the warehouse ID (simpler)
DATABRICKS_WAREHOUSE_ID=your-warehouse-id

# Option 2: OR provide the full HTTP path (if you prefer)
# DATABRICKS_HTTP_PATH=/sql/1.0/warehouses/your-warehouse-id

# For local development with PAT (Optional - not needed if using service principal)
# Generate a PAT in Databricks: User Settings -> Developer -> Access Tokens
DATABRICKS_ACCESS_TOKEN=your-databricks-access-token

# OAuth2 Service Principal Credentials (for local testing)
# In Databricks Apps, these are auto-injected by the platform
# For local dev: Create a service principal in Account Console -> Service Principals
DATABRICKS_CLIENT_ID=your-service-principal-client-id
DATABRICKS_CLIENT_SECRET=your-service-principal-client-secret

# Catalog and Schema (default: samples.wanderbricks)
DATABRICKS_CATALOG=samples
DATABRICKS_SCHEMA=wanderbricks

# ===== API SETTINGS =====
API_PREFIX=/api
DEBUG=false
LOG_LEVEL=INFO
