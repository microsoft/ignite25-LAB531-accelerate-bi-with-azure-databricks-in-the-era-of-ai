# Example environment file for local development
# Copy this to backend/.env and update with your values

# ===== DATABRICKS SQL WAREHOUSE CONNECTION =====
# Required: Your Databricks workspace server hostname (without https://)
DATABRICKS_SERVER_HOSTNAME=your-workspace.azuredatabricks.net

# Option 1: Provide just the warehouse ID (simpler)
DATABRICKS_WAREHOUSE_ID=your-warehouse-id

# Option 2: OR provide the full HTTP path (if you prefer)
# DATABRICKS_HTTP_PATH=/sql/1.0/warehouses/your-warehouse-id

# Required: Personal Access Token for local development
DATABRICKS_ACCESS_TOKEN=your-personal-access-token

# Catalog and Schema (default: samples.wanderbricks)
DATABRICKS_CATALOG=samples
DATABRICKS_SCHEMA=wanderbricks

# FastAPI settings
DEBUG=True
API_PREFIX=/api

# Python Logging Configuration
LOG_LEVEL=DEBUG
